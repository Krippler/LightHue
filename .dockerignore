.git
.github
tests
data
*.md
!README.md
pytest.ini
ruff.toml
requirements-dev.txt
__pycache__
*.py[cod]
.venv
venv
.pytest_cache
.ruff_cache
# Release metadata for the Community Applications listing. The
# Dockerfile copies named directories, so these were never in the
# image — this just keeps them out of the build context.
screenshots
templates
ca_profile.xml
