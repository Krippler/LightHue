[pytest]
testpaths = tests
# Put the repo root on sys.path so `pytest` works the same as `python -m
# pytest`, which gets it for free from the working directory.
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
filterwarnings =
    error::DeprecationWarning:app.*
