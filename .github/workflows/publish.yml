name: Publish image

# Unraid installs from a registry rather than building locally, so every push
# to main publishes an image its template can pull.
#
# Work branches publish too, under their own branch-named tag. Testing a fix
# against real hardware otherwise means merging it to main first, which is a
# bad trade: the change that needs proving is exactly the one that should not
# be on the default branch yet. `latest` stays gated on the default branch
# below, so nothing an ordinary install pulls is affected.
on:
  push:
    branches: ["main", "claude/**", "fix/**", "feat/**"]
    tags: ["v*"]
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Derive image tags
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=ref,event=branch
            type=ref,event=tag
            type=sha,format=short

      - uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          # Unraid is x86_64; add linux/arm64 here if you run this elsewhere.
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
